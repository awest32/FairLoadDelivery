# Legacy Scripts
  
  Pre-defense experiment scripts and debug code. Quarantined from active codebase
  during the cleanup-for-defense refactor. Not maintained. May not run as written
  due to API drift in src/.
  
  Restored to active use only if a specific experiment needs to be re-run. Otherwise,
  prefer the harness in experiments/ for new experimental work.